<!DOCTYPE html>
<html lang="tr">
<head>
  <meta charset="UTF-8" />
  <title>Recovai | Antrenör Girişi</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <style>
    body {
      margin: 0;
      height: 100vh;
      font-family: Inter, Arial, sans-serif;
      background: linear-gradient(135deg, #1f3c88, #3a6ee8);
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .login-card {
      background: #fff;
      width: 100%;
      max-width: 420px;
      border-radius: 16px;
      padding: 36px 32px;
      box-shadow: 0 20px 60px rgba(0,0,0,0.25);
    }

    .logo {
      text-align: center;
      font-size: 26px;
      font-weight: 800;
      color: #1f3c88;
      margin-bottom: 6px;
    }

    .subtitle {
      text-align: center;
      font-size: 14px;
      color: #666;
      margin-bottom: 28px;
    }

    .form-group {
      margin-bottom: 18px;
    }

    label {
      display: block;
      font-size: 13px;
      color: #555;
      margin-bottom: 6px;
    }

    input {
      width: 100%;
      padding: 12px 14px;
      border-radius: 10px;
      border: 1px solid #ddd;
      font-size: 14px;
      outline: none;
    }

    input:focus {
      border-color: #3a6ee8;
      box-shadow: 0 0 0 2px rgba(58,110,232,0.15);
    }

    .login-btn {
      width: 100%;
      margin-top: 10px;
      padding: 14px;
      border-radius: 12px;
      border: none;
      background: linear-gradient(135deg, #1f3c88, #3a6ee8);
      color: #fff;
      font-size: 15px;
      font-weight: 600;
      cursor: pointer;
    }

    .login-btn:hover {
      opacity: 0.95;
    }

    .footer-text {
      margin-top: 22px;
      text-align: center;
      font-size: 12px;
      color: #888;
    }

    .error {
      background: #fdecea;
      color: #c0392b;
      padding: 10px 12px;
      border-radius: 8px;
      font-size: 13px;
      margin-bottom: 14px;
      text-align: center;
    }

    @media (max-width: 480px) {
      .login-card {
        border-radius: 0;
        height: 100vh;
        max-width: none;
        display: flex;
        flex-direction: column;
        justify-content: center;
      }
    }
  </style>
</head>

<body>

  <div class="login-card">

    <div class="logo">Recovai</div>
    <div class="subtitle">Antrenör Paneli Girişi</div>

    <% if (typeof error !== 'undefined') { %>
      <div class="error"><%= error %></div>
    <% } %>

    <form method="POST" action="/login/loginPost">
      <div class="form-group">
        <label>E-posta</label>
        <input
          type="email"
          name="email"
          placeholder="coach@recovai.com"
          required
        />
      </div>

      <div class="form-group">
        <label>Şifre</label>
        <input
          type="password"
          name="password"
          placeholder="••••••••"
          required
        />
      </div>

      <button type="submit" class="login-btn">
        Giriş Yap
      </button>
    </form>

    <div class="footer-text">
      © <%= new Date().getFullYear() %> Recovai
    </div>

  </div>

</body>
</html>
